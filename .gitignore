dist/
build/
*.exe
__pycache__/
.env

# Local toolchains downloaded during Android/Gradle troubleshooting -- large
# binaries that should never live in git history; re-fetch on demand instead.
.gradle-local/
.jdk-local/
gradle-*-bin.zip
temurin*.zip

# Python virtualenvs and test/coverage artifacts
venv/
.venv/
.coverage
.pytest_cache/
htmlcov/
coverage.xml

# Ad hoc build/troubleshooting logs generated while debugging locally
*log*.txt

# Local-only runtime state for the airgap_attestation submission API
submissions.db
airgap_attestation/demo_bundle.json

# Android: regeneratable build/cache output and machine-specific SDK path
# (local.properties hardcodes an absolute path to this machine's Android SDK).
android/.gradle/
android/app/build/
android/.idea/
android/local.properties


